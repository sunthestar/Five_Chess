define
焦点			p
焦点指针		focus

棋盘数组（初始化）		Map
棋盘数组大小				XLen	YLen

棋盘数组（落子）

当前棋子颜色


main.c
主循环（显示）

Key.c
获取键盘输入（每输入一次刷新一次棋盘显示）
方向键控制移动（焦点移动 刷新棋盘 创建选择点函数 刷新棋盘）
空格键落子（棋子颜色切换）

Map.c
初始化棋盘

Show.c
打印棋盘数组

Login.c
焦点移动函数（在棋盘上生成焦点的选择框）
落子函数

当前焦点		define.h	select_Point_
当前棋子颜色  黑白轮切

有落子的棋盘（）


输赢判定（根据当前焦点位置判定 当前焦点落子后调用判定函数）
